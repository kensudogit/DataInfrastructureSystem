"""Django project placeholder for teams preferring Django admin/reporting.

This package uses FastAPI as the primary API. If Django is required,
install django and scaffold with:

  django-admin startproject config apps/django_app
  python manage.py startapp reporting

Recommended: reuse packages.collectors / packages.transformers from Django
management commands.
"""
